# Resultados de build
dist/
build/
*.spec
.venv-build/

# Python
__pycache__/
*.py[cod]

# Dados locais (config guarda o seu @; gravações são pesadas)
config.json
Gravacoes/
Teste1/
ProgramaVersao1/
.thumbs/
*.ttgifts

# libmpv (112 MB) - baixada pelo script de build, nao versionada
mpvlib/

# Quadros e videos de referencia usados para medir o chat (255 MB)
.analysis_frames/

# Conjunto de emoji (13 MB) - gerado por emoji_pack.py, chamado pelo build
assets/emoji.zip

# Sistema
.DS_Store
Thumbs.db
